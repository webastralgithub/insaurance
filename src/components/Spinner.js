import React from 'react';
import './Spinner.css'; // Remember to create a CSS file for styling

const Spinner = () => {
  return (
    <div className="spinner">
      <div className="spinner-inner"></div>
    </div>
  );
};

export default Spinner;
